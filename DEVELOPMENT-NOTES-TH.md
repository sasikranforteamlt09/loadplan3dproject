# บันทึกการพัฒนา — เขียนใหม่ด้วย React (LoadPlan 3D v2.0)

เอกสารนี้บันทึกการย้ายเว็บแอปจากรุ่นวานิลลา (`เว็บแอป_v2/index.html`, `app.html`, `pack.js`)
มาเป็นโครงการ React + TypeScript ที่ `/home/claude/loadplan3d/`
**ผลการคำนวณและเงื่อนไขทั้งหมดไม่เปลี่ยน** สิ่งที่เปลี่ยนคือโครงสร้างโค้ดและหน้าตาการใช้งาน

---

## 1. เทคโนโลยีและรุ่นที่ใช้

| ส่วน | รุ่น | หมายเหตุ |
|---|---|---|
| Vite | 5.4.11 | ตัว build |
| React | 18.3.1 | |
| TypeScript | 5.6.3 | เปิดโหมด strict |
| Tailwind CSS | 3.4.17 | รุ่น 3 ไม่ใช่รุ่น 4 |
| three.js | 0.160.1 (r160) | เรียกใช้โดยตรงใน component ไม่ใช้ react-three-fiber |
| react-router-dom | 6.30.1 | BrowserRouter + `public/_redirects` |
| qrcode | 1.5.4 | ใช้เฉพาะตอน build สร้าง `public/qr.svg` |
| @types/three | 0.160.0 | |

รวมไลบรารีทั้งหมดไว้ในชุดไฟล์ที่ build ออกมา **ไม่เรียก cdnjs และไม่เรียก Google Fonts**
(รุ่นวานิลลาเรียกทั้งสองอย่าง) ตัวอักษรใช้ฟอนต์ระบบของเครื่องผู้ใช้
ขนาดไฟล์หลังบีบอัด: JavaScript ประมาณ 185 KB (gzip) และ CSS ประมาณ 5 KB (gzip)

## 2. สิ่งที่เปลี่ยนจากรุ่นวานิลลา

### 2.1 แกนคำนวณ
- ย้าย `pack.js` มาเป็น `src/lib/pack.ts` **แบบบรรทัดต่อบรรทัด** คงคำอธิบายภาษาไทยไว้ทั้งหมด
  เพิ่มเฉพาะชนิดข้อมูล (type) ของ TypeScript ไม่แตะลำดับการวนซ้ำหรือการเปรียบเทียบใด ๆ
- เงื่อนไขที่ยังบังคับใช้ครบ: วางขนานแกน อยู่ในขอบเขต ไม่ซ้อนทับ ไม่เกินน้ำหนักบรรทุก
  ฐานรองรับ 100% ไม่วางของหนักทับของเบา โซนกันท้ายรถ หมุนได้ 6 ทิศทาง
  เรียงตามน้ำหนักแล้วตามปริมาตร สะท้อนแกนความลึก เรียงลำดับแบบพึ่งพา และจับเวลาที่ใช้คำนวณ

### 2.2 โครงสร้างหน้าเว็บ
- แยกเป็นสองเส้นทาง `/` (หน้าแรก) และ `/app` (เครื่องมือ 3 ขั้นตอน) ด้วย react-router-dom
  แบบไม่มี `#` ในที่อยู่เว็บ พร้อมไฟล์ `public/_redirects` (`/*  /index.html  200`) สำหรับ Cloudflare Pages
- แยกส่วนติดต่อผู้ใช้เป็น component: `Stepper`, `Step1Truck`, `Step2Items`, `Step3Result`,
  `Viewer3DCanvas`, `HeroScene`, `Layout` และแยกการวาดภาพเป็น `lib/viewer3d.ts` กับ `lib/wallview.ts`

### 2.3 หน้าตาและการใช้งาน
- ทำชุดตัวแปรสีใน `tailwind.config.js` จากสีประจำระบบเดิม กรมท่า `#1f3c58` และส้ม `#f59e0b`
  (รุ่นวานิลลาใช้ `#f28c28` ปรับมาใช้ `#f59e0b` ตามที่กำหนด) พร้อมชั้นปุ่ม การ์ด และช่องกรอกที่ใช้ซ้ำได้
- **ปุ่ม + / − ขยายจาก 38 พิกเซล เป็น 44 พิกเซล** ตามเกณฑ์พื้นที่สัมผัสขั้นต่ำ
  ปุ่มและช่องกรอกอื่นทุกจุดสูงอย่างน้อย 44 พิกเซลเช่นกัน
- ตารางกลุ่มขนาดบนจอแคบ ย้ายปุ่ม "ลบ" เข้าไปอยู่ในช่องชื่อกลุ่ม จึงไม่ต้องเลื่อนตารางแนวนอนอีกต่อไป
- แถบขั้นตอนใช้ป้ายสั้นบนจอแคบ (เลือกรถ / นับพัสดุ / แผนจัดวาง) กันข้อความไทยตัดบรรทัดจนแถบสูงเกินไป
- ใส่ป้ายกำกับ (`aria-label`) ให้ช่องกรอกและปุ่มทุกตัวในตาราง แท็บใช้ `role="tab"` และ `aria-selected`
- ไม่มีอีโมจิในหน้าจอใด ๆ
- เพิ่มคำเตือนในขั้นที่ 2 เมื่อปริมาตรพัสดุรวมเกินปริมาตรตู้
- เปลี่ยนชื่อแท็บ "มุมมองรายชั้น" เป็น "มุมมองรายผนัง" ให้ตรงกับสิ่งที่แสดงจริง

### 2.4 หน้าแรก
- เพิ่มส่วน **ภาพหน้าจอจริง** สามภาพ ถ่ายจากระบบรุ่นที่ build แล้วด้วย Playwright เก็บไว้ที่ `public/screenshots/`
- เพิ่มส่วน **รหัสคิวอาร์** สร้างเป็นไฟล์ SVG ตอน build จากค่า `DEPLOY_URL` ใน `src/config.ts`
  ด้วยไลบรารี `qrcode` ที่ติดตั้งจาก npm ไม่เรียกบริการสร้างคิวอาร์ภายนอก
- ท้ายหน้ายังคงเครดิตปริญญานิพนธ์ตามเดิม และไม่ระบุชื่อบริษัทใด

### 2.5 การพิมพ์
- คงพฤติกรรมเดิมทุกข้อ: กระดาษ A4 ขอบ 12 มม. ตารางลำดับพิมพ์ครบทุกแถว หัวตารางซ้ำทุกหน้า
  แปลงภาพสามมิติเป็นรูปภาพ (`toDataURL`) ก่อนพิมพ์เพื่อไม่ให้ออกมาเป็นหน้าว่าง
  และพิมพ์ทั้งภาพสามมิติกับมุมมองรายผนังเสมอ ไม่ขึ้นกับแท็บที่เปิดอยู่

### 2.6 ข้อมูล
- ไม่มี localStorage ไม่มีคุกกี้ ไม่มีเครื่องแม่ข่าย ไม่มีฐานข้อมูล ไม่มีระบบล็อกอิน (เหมือนรุ่นวานิลลา)

## 3. ผลการทดสอบ

### 3.1 ชุดทดสอบแกนคำนวณ (`npm test` — 29 ชุดข้อมูล วางได้รวม 6,688 ชิ้น)

ดัดแปลงจาก `/tmp/lp/test.js` ของรุ่นวานิลลา เรียกแกนคำนวณ TypeScript ผ่าน `tsx`

| รายการตรวจ | รุ่นวานิลลา (หลังแก้) | รุ่น React |
|---|---|---|
| ซ้อนทับ | 0 | **0** |
| ออกนอกขอบเขต (รวมโซนกันท้าย) | 0 | **0** |
| ฐานรองรับไม่ถึง 100% | 0 | **0** |
| ชิ้นหนักอยู่บนชิ้นเบา | 0 | **0** |
| เกินน้ำหนักบรรทุก | 0 | **0** |
| ชิ้นที่รองรับอยู่หลังชิ้นด้านบนในลำดับ | 0 | **0** |
| ชิ้นด้านหน้าบังชิ้นที่ลึกกว่าในลำดับ | 0 | **0** |
| จำนวนชุดที่พบปัญหา | 0 / 29 | **0 / 29** |
| จำนวนชิ้นที่วางได้รวม | 6,688 | **6,688** |

### 3.2 การเทียบผลกับรุ่นวานิลลาแบบตัวต่อตัว

รันทั้ง `pack.js` เดิมและ `pack.ts` ใหม่ด้วยข้อมูลชุดเดียวกันทั้ง 29 ชุด
แล้วเทียบ ลำดับ ชื่อ พิกัด x y z ขนาดที่วาง น้ำหนัก จำนวนชิ้นที่วางไม่ได้ ความยาวที่ใช้
อัตราการใช้ประโยชน์ ปริมาตรว่างคงเหลือ และปริมาตรโซนกันท้าย

> **ผลต่าง 0 ชุดจาก 29 ชุด** — ผลลัพธ์ตรงกันทุกตัวเลข

### 3.3 การทดสอบผ่านเบราว์เซอร์จริง (Playwright + Chromium)

ทดสอบกับไฟล์ที่ build แล้วใน `dist/` สามขนาดหน้าจอ

| ขนาดหน้าจอ | ผลการทดสอบ |
|---|---|
| 390 × 844 (มือถือ) | ผ่านทุกข้อ |
| 360 × 740 (มือถือจอเล็ก) | ผ่านทุกข้อ |
| 1280 × 800 (คอมพิวเตอร์) | ผ่านทุกข้อ |

ข้อที่ตรวจในแต่ละขนาด
- เปิดหน้าแรก → กดลิงก์ "เริ่มใช้งาน" → เข้าหน้า `/app` ได้ (เส้นทางไม่มี `#`)
- กด "ใส่ข้อมูลตัวอย่าง" → กด + สองครั้ง กด − หนึ่งครั้ง จำนวนเปลี่ยนจาก 40 เป็น 41 ถูกต้อง
- ปุ่ม + / − มีพื้นที่สัมผัสไม่ต่ำกว่า 44 × 44 พิกเซล
- กด "คำนวณแผนการจัดวาง" → ได้ **วางได้ 151 ชิ้น อัตราการใช้ประโยชน์ 78.50% ความยาวที่ใช้ 62.0 ซม.**
  ตรงกับรุ่นวานิลลา
- **จำนวนแถวในตารางลำดับ = 151 แถว เท่ากับจำนวนชิ้นที่วางได้พอดี**
- สลับแท็บมุมมองสามมิติ ↔ มุมมองรายผนัง ได้ปกติ
- ไม่มีข้อผิดพลาดใน console เลยทั้งสามขนาด
- ไม่มีการเลื่อนหน้าจอแนวนอน (ข้อความไทยไม่ล้นออกนอกจอ)

การทดสอบการพิมพ์ (จำลองเป็นไฟล์ PDF ขนาด A4)
- ได้ PDF จำนวน **7 หน้า**
- มีรูปภาพสามมิติฝังอยู่จริง (แปลงเป็น `data:image/png` แล้ว ไม่ใช่หน้าว่าง)
- **มีแถวลำดับครบ 1 ถึง 151 ไม่ขาดแถวใดเลย**

ภาพหน้าจอผลการทดสอบทั้งหมดอยู่ที่ `screenshots-test/`
(ตรวจด้วยสายตาแล้ว ไม่พบข้อความไทยล้นกรอบหรือปุ่มถูกตัด)

## 4. สิ่งที่ยังไม่ได้ทำ

- **ขนาดตู้รถ 4 ล้อยังเป็นค่าสมมติ** (300 × 170 × 180 ซม.) รอค่าที่วัดจริงจากหน้างาน
  ระบบยังขึ้นคำเตือนกำกับไว้ทุกครั้ง ห้ามนำตัวเลขชุดนี้ไปใส่ในเล่ม
- มุมมองรายผนังยังจัดกลุ่มตามค่าพิกัด x ของกล่องแต่ละชิ้นเหมือนรุ่นวานิลลา
  ยังไม่ได้จัดกลุ่มเป็น "ชั้น" ตามที่ผู้ปฏิบัติงานเข้าใจ
- ยังไม่ได้แยกข้อความเตือน "เกินความจุ" ออกจาก "เกินน้ำหนักบรรทุก" ในหน้าผลลัพธ์
- ยังไม่มีหมายเลขรุ่นแสดงท้ายหน้า และยังไม่มีการแจ้งเตือนเมื่อแถวที่วางจาก Excel ถูกข้าม
- ยังไม่ได้ทดสอบบนเครื่องโทรศัพท์จริงและบนเบราว์เซอร์ Safari (ทดสอบได้เฉพาะ Chromium ในแซนด์บ็อกซ์)
- ยังไม่ได้นำขึ้น Cloudflare Pages จริง จึงยังไม่ได้ยืนยันว่ารหัสคิวอาร์ชี้ไปยังที่อยู่ที่ใช้งานได้
  (ต้องแก้ค่า `DEPLOY_URL` ใน `src/config.ts` หลังได้ที่อยู่จริงก่อน)
- ยังไม่ได้แบ่งชุดไฟล์ JavaScript ออกเป็นหลายก้อน (three.js ทำให้ไฟล์หลักใหญ่ประมาณ 185 KB หลังบีบอัด)
